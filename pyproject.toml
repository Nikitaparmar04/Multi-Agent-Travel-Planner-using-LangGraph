[project]
name = "multi-agent-trip-planeer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandas>=3.0.3",
]

[tool.coverage.run]
omit = [
    ".venv/*",
    "tests/*",
    "setup.py"
]

